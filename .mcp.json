{
  "mcpServers": {
    "skyagent": {
      "command": "bun",
      "args": [
        "./scripts/mcp-server.ts",
        "--cache-runtime"
      ],
      "env": {
        "HYPIXEL_API_KEY": "${HYPIXEL_API_KEY}"
      }
    }
  }
}
